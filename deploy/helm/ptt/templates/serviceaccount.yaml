{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ptt.serviceAccountName" . }}
  labels:
    {{- include "ptt.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
